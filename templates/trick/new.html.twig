{% extends 'base.html.twig' %}

{% block title %}New Trick{% endblock %}

{% form_theme form 'trick/_collection_form.html.twig' %}

{% block body %}

    <div class="container mt-5">
        <h1>Ajouter une nouvelle figure</h1>
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="btn">{{ button_label|default('Créer une nouvelle figure') }}</button>
        {{ form_end(form) }}
    </div>
{% endblock %}

{# javascripts #}
{% block javascripts %}
    <script type="text/javascript" src="/js/trick.js"></script>
{% endblock %}
